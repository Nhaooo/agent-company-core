# Custom tool

Demonstrates a registered low-risk tool passing through policy, STOP, and
audit boundaries.

Prerequisites: core package. Run `python examples/custom_tool/main.py`.
Expected behavior: the provider asks for the registered tool and the engine
returns its harmless structured output.
