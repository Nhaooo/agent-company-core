# FastAPI integration

Demonstrates the optional reference app through an in-process ASGI client.

Prerequisites: `python -m pip install "agent-company-core[app]"`. Run
`python examples/fastapi_integration/main.py`.

Expected behavior: the local app returns a healthy response without starting a
server or making a network call.
