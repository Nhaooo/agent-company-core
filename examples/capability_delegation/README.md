# Capability delegation

Demonstrates agent selection from declared capabilities rather than message
keywords.

Prerequisites: core package. Run `python examples/capability_delegation/main.py`.
Expected behavior: the `research` capability selects the neutral researcher
agent and records the delegation in durable mission state.
