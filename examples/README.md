# Examples

Run these from the repository root after installing the package (or after
`uv sync --all-extras --dev`). Offline examples do not need credentials or
network access. Provider examples use injected clients by default; pass
`--live` only when you deliberately want to call a configured local or hosted
provider.
