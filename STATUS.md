# Status

The local OSS extraction is functionally complete for its alpha scope and is
in its alpha scope and has been published. The public GitHub repository and
PyPI package `agent-company-core==0.1.0` are live. The core test suite, Ruff,
mypy, package build, Twine metadata validation, pip-audit, fallback secret
scan, official Gitleaks scans, release workflow, and fresh PyPI installation
smoke test have passed. The restricted runner remains disabled by default and
is explicitly not a hostile-code sandbox.
